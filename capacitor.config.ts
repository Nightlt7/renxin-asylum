import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lantian.renxin_asylum',
  appName: '仁馨精神病院',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
